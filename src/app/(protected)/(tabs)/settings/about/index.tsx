import { AboutScreen } from "@/features/settings";

/**
 * Render the settings route that displays the AboutScreen component.
 *
 * @returns A JSX element rendering the AboutScreen
 */
export default function AboutScreenRoute() {
  return <AboutScreen />;
}