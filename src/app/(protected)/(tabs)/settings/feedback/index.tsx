import { FeedbackScreen } from "@/features/settings";


/**
 * Render the settings route that displays the FeedbackScreen component.
 *
 * @returns A JSX element rendering the FeedbackScreen
 */
export default function FeedbackScreenRoute() {
  return <FeedbackScreen />;
}