import { ScreenSettings } from "@/features/settings";

/**
 * Render the settings route that displays the ScreenSettings component.
 *
 * @returns A JSX element rendering the ScreenSettings
 */
export default function SettingsScreenRoute() {
  return <ScreenSettings />;
}