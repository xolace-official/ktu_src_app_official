import { ProfileAccountScreen } from "@/features/settings";


/**
 * Render the settings route that displays the ProfileAccountScreen component.
 *
 * @returns A JSX element rendering the ProfileAccountScreen
 */
export default function ProfileAccountScreenRoute() {
  return <ProfileAccountScreen />;
}