import { DeveloperCreditScreen } from "@/features/settings";


/**
 * Render the settings route that displays the DeveloperCreditScreen component.
 *
 * @returns A JSX element rendering the DeveloperCreditScreen
 */
export default function DeveloperCreditScreenRoute() {
  return <DeveloperCreditScreen />;
}