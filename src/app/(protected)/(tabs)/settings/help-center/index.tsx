import { HelpCenterScreen } from "@/features/settings";


/**
 * Render the settings route that displays the HelpCenterScreen component.
 *
 * @returns A JSX element rendering the HelpCenterScreen
 */
export default function HelpCenterScreenRoute() {
  return <HelpCenterScreen />;
}