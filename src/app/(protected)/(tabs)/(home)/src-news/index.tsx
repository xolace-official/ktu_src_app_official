import { NewsScreen } from "@/features/src-news";

/**
 * Route component that renders the NewsScreen for the protected home tab.
 *
 * @returns The rendered NewsScreen React element.
 */
export default function NewsScreenRoute() {
  return <NewsScreen />;
}