import { CategoryScreen } from '@/features/marketplace';

/**
 * Renders the CategoryScreen for the dynamic category route.
 *
 * @returns A JSX element that renders the `CategoryScreen` component.
 */
export default function CategoryScreenRoute() {
  return <CategoryScreen />;
}