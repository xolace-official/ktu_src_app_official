import { ProductScreen } from '@/features/marketplace';

/**
 * Render the marketplace product screen.
 *
 * @returns The JSX element that renders the marketplace product screen.
 */
export default function ProductScreenRoute() {
  return <ProductScreen />;
}