import { MarketplaceScreen } from '@/features/marketplace';

/**
 * Render the marketplace route that displays the MarketplaceScreen component.
 *
 * @returns A JSX element rendering the MarketplaceScreen
 */
export default function MarketplaceScreenRoute() {
  return <MarketplaceScreen />;
}