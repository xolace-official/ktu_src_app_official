import NewProductForm from '@/features/marketplace/forms/new-product-form';

/**
 * Render the new product creation form for the marketplace.
 *
 * @returns A React element that renders the `NewProductForm` component.
 */
export default function NewProductRoute() {
  return <NewProductForm />;
}